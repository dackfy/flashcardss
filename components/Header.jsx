const React = require('react');

function Header() {
  return (
    <header>
      <ul>
        <li>
          <a href="/">Главная</a>
        </li>
      </ul>
    </header>
  );
}

module.exports = Header;
